# Spec

/spec/_rules

这里放置前端和后端一些 rules，这部分感觉可以封装成 skill，避免 agent 缺少，这个 skill 需要放在 agent 里面，这样避免其他人agent 携带不需要的 skill 导致增加了上下文

- api.md
- error_code.md
- redis_key.md
- auth.md

我们这里设计两套设计规范，形成合适的后续的想法开发的规范和标准

后端：
go + python

前端：
react + shadcn ui
UI（pencil）
Pencil 原型 → 自动生成 UI Spec → 自动生成 API → 自动生成前端页面（React/Flutter）」
