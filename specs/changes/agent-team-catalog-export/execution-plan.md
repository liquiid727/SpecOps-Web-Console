# Execution Plan

1. Add `agent_team` to the `spec-web-ui` catalog model and discovery helpers.
2. Create the new `agent-teams/` asset directory and normalize the imported governance pack into it.
3. Add `/agent-teams` route, nav entry, locale copy, and route tests.
4. Extend export/install mapping so `agent-teams/...` remains namespaced in snapshots and bundles.
5. Add test coverage for catalog filters, template routes, workspace selection, export manifests, and CLI install compatibility.
