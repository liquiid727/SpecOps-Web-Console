# Test Plan — CLI-GUI-032 / Issue 107

Run the available route/composer/attempt component and runtime tests plus static gates. Full Provider → Deployment → Route → Session → failure/fallback → recovery browser journey, screenshots/traces, viewport checks, and secret canary scan are required.

