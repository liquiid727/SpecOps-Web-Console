# Test Plan — CLI-GUI-032 / Issue 103

Run provider/deployment settings component and runtime tests, then typecheck, UI governance, build, and `npx specos check`. Browser CRUD, readonly/retry, write-only secret negative checks, and screenshots/traces remain required gates.

