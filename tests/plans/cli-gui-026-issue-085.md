# Issue 085 independent test plan

Scope: automatic sync ordering, five-minute TTL behavior, read-only/no-spawn constraints, failure fallback, manual bypass, and browser model-selector visibility.

Result: backend checks passed and the isolated browser model-selector scenario passed with screenshot/trace evidence. See `tests/results/cli-gui-026.issue-085.local.json` and `tests/results/cli-gui-026.issue-085.browser.raw.json`.
