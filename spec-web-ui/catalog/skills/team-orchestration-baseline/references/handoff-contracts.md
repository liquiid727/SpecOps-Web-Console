# Handoff Contracts

Reusable team baselines should standardize:

- routing
- task plan
- handoff
- findings
- sync handoff

Each artifact should keep stable section names so later agents or humans can consume them predictably.
