# Exports Feature

Web adapters for bundle generation, file-system snapshots, diff review, and
review-state persistence.

- `server.ts`: server-side export workflow and review persistence
- `client.ts`: client-safe review-note helpers

Bundle planning and install-target configuration live in `@specos/bundler`.
