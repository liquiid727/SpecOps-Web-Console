# Catalog Feature

Web adapters for Catalog storage and Discover preferences.

- `server.ts`: loads registry configuration and reusable asset sources
- `preferences.ts`: persists favorites, comparisons, and preset display state
- `dnd.ts`: pure drag-and-drop placement behavior
- `ui.ts`: Catalog-specific presentation helpers

Catalog filtering and ranking live in `@specos/catalog`.
