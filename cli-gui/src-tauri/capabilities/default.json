{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege WebView capability set. Sidecar supervision stays host-owned; notification and clipboard use WebView APIs.",
  "windows": ["main"],
  "permissions": [
    "core:app:default",
    "core:window:default",
    "dialog:allow-open",
    "dialog:allow-message"
  ]
}
