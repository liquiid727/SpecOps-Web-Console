# LiteSpec

`LiteSpec` is the default feature-driven mode.

Keep feature context close together and keep the active workspace small:

```text
README.md
current/
design/
.features/
  roadmap.md
  _draft/
  RP-001-example/
.agents/
```
