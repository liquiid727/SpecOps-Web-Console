# @specos/installer

Applies a validated SpecOS bundle manifest to a target project and writes the
installed-bundle record.

Manifest parsing and schema validation happen before this interface. The module
owns recursive file installation and installation evidence.
