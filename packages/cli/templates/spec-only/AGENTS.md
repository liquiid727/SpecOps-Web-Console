# SpecOS Agent Instructions

This template is draft/spec-first.

- Treat `spec-draft/` as the editable source for intent.
- Treat `spec/` as the accepted bundle for normalized output.
- Keep generated tests and reports traceable to the active spec.
