# Current Specs

Accepted Lens Fitting specs are split into branches:

- `product/`
- `architecture/`
- `database/`
- `api/`
- `ui/`

The combined machine-readable draft baseline is `lens-fitting.spec.yaml`.
