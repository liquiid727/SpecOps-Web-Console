# Tests

Tests are derived from Spec branches, not from implementation details.

Use:

- `plans/` for normalized plans.
- `scenarios/` for user and system journeys.
- `results/` for normalized evidence.
