# LiteSpec Overlay: OpenAPI Agent

- Keep contracts feature-local and implementation-ready; avoid broad versioning or platform governance expansion unless the spec explicitly needs it.
- Prefer one feature's request, response, error, and example set over cross-epic API redesign.
