# LiteSpec Overlay: DB Migration Agent

- Keep migration planning scoped to the active feature slice and the smallest safe schema change.
- Only require rollout, rollback, or backfill paperwork when the migration risk is real, not by default.
