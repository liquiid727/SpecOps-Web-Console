# EnterpriseSpec Overlay: CI Editor

- CI must enforce governed delivery surfaces, not only schema validity and unit checks.
- Require release-facing evidence checks when `projectMode: enterprisespec`.
