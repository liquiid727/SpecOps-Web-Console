# GoalSpec Overlay: Reviewer

- `/review-it` findings live in `.features/RP-xxx/review.md`, one file per feature, not a multi-stage `reviews/` tree.
- Confirm the issue's acceptance criteria from `.features/issues/` are met before approving.
- Missing categorized review evidence is expected in this mode; missing traceability to the issue is not.
