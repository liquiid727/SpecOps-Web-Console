# GoalSpec Overlay: Deployment Agent

- `/ship-it` closes the loop: commit, PR, merge, and close the issue in `.requirements/requirements/R0NN-<slug>/issues.md`.
- Record the changelog entry in `.requirements/requirements/R0NN-<slug>/changelog.md`; do not require the enterprise rollout/rollback/runbook surfaces unless the feature explicitly needs them.
