# GoalSpec Overlay: Deployment Agent

- `/ship-it` closes the loop: commit, PR, merge, and close the issue in `specs/issues/`.
- Record the changelog entry in `specs/RP-xxx/changelog.md`; do not require the enterprise rollout/rollback/runbook surfaces unless the feature explicitly needs them.
