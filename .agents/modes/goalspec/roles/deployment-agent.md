# GoalSpec Overlay: Deployment Agent

- `/ship-it` closes the loop: commit, PR, merge, and close the issue in `.features/issues/`.
- Record the changelog entry in `.features/RP-xxx/changelog.md`; do not require the enterprise rollout/rollback/runbook surfaces unless the feature explicitly needs them.
