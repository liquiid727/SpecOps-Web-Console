# Current

`current/` stores the smallest active delivery context for the repository.

Use it for current status, blockers, handoff notes, and the active feature when that context needs to stay explicit for agents.
