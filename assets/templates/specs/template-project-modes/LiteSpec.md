# LiteSpec

`LiteSpec` is the default feature-driven mode.

Use it when:

- one engineer or one agent can finish a feature end to end
- token efficiency matters
- review and test evidence can stay close to the feature
