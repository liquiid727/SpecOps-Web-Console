# Spec Handoff Template

```markdown
## Spec Handoff
direction_path: <direction.md 路径，没有则写 none>
execution_path: <execution.md 路径，没有则写 none>
constraints:
  - <实施方必须遵守的约束，没有则写 none>
blocked_paths:
  - <明确禁止写入的路径，没有则写 none>
residual_risks:
  - <剩余风险，没有则写 none>
next_agent: architect | spec-writer | reviewer | backend/<context> | test | sync | none
```
