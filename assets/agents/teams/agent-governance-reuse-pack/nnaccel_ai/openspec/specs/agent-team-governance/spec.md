# Agent Team Governance

Imported agent team packs provide reusable routing and governance assets without taking over the host project automatically.

## Core Rules

- Keep the pack namespaced under `agent-teams/<team-id>/`.
- Reference the pack explicitly from the host project's own agent entrypoints.
- Update routing catalogs and governance specs together when adding durable roles.
