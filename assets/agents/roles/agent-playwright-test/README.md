# Scenarios

Business-flow and multi-step scenario test assets belong here.

Scenario assets should map back to entries in the selected child package's
`evidence/plans/` and forward to normalized results in `evidence/artifacts/`.
