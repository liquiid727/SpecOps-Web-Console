# Naming And Shape

- define one family prefix per concern
- include the primary identifier in a stable position
- distinguish queue, lease, projection, and rate-limit families by prefix
- document value structure: string, hash, set, list, stream, or JSON blob
