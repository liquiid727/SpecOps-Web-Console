# Skill Catalog

Skill assets for the SpecOS web UI belong here.

This directory is the tool-site catalog source. It is separate from project-level `skills/developer/` folders and user-level `~/.codex/skills/` installs.
