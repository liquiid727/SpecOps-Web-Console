# Concurrency Tests

This directory stores spec-derived concurrent scenario and consistency-invariant assets.

Concurrency tests must record actor count, request count, invariant, expected final state, observed final state, and gate impact in normalized `tests/results/*.json` artifacts.
