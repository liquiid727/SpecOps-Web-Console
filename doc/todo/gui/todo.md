1. 将UI效果做成和claude code 和 codex app一致
- 三栏布局：左侧是session管理 中间是会话记录和对话窗口，可以查看终端的会话消息和内容以及发送新的消息；右侧是diff/git/
  - 都具备左右两侧隐藏和弹出的按钮
- 类似claude code codex 等app，左侧是session管理区域，可以根据时间、项目、recent等方式进行会话分组管理和排序
  - 点击开启workspace/项目，可以点击打开本地的目录，倒入对应的目录地址信息，不需要用户手动输入变量
  - session可以进行点击查看、右键点击可以 置顶、归档、完成、fork
  - 左侧session最下面固定有一个icon+设置/setting，可以点开进行一些配置；具体的配置暂时搁置，这个配置一般是配置比如宠物、模型设置、账户、hook、git、环境、个性化、外观、快捷键等等
  - 可以左侧点击可以新增session，左侧的session管理可以自己拖拽顺序、自定义session名称
- 进入session，在中间下方的对话栏进行输入我们的对应的内容
  - 中间区域是我们的session的对话内容，最下方有一个输入端聊天区域，可以在这个区域输入内容，更换权限、模式、模型
  - 中间的session可视化区域，是终端的历史工作内容
    - 优化，对于返回的得到的md内容，这里借助md-preview等可视化的插件可视化md，尤其是plan模式或者skill会返还md的内容
-右侧预览区域
- 项目文件语言
- diff预览
- git预览
- 



2. 打包桌面端生成


3. 主题- hardness增设 - code starrail
根据崩坏星穹铁道的主题，内置一整套主题结构
根据其游戏的历史背景
比如 bug：若虫
比如 来古士：超级管理员agent
增设几个人物比如
主角：星穹：具备开拓，探索的潜质，是一个 xx
三月七：
丹恒：搜索agent/文库agent/ 专门负责处理文本工作
星期日：负责协调多个agent
