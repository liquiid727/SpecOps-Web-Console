# GoalSpec Canonical Overlay: Spec Editor

- Drive `/prd` and optional `/prd-to-spec`, then `/to-issues` to produce small, single-`/goal`-sized issues in `.requirements/requirements/R0NN-<slug>/issues.md`.
