{
  "name": "specos-ai",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @specos/core && npm run build --workspace @specos/cli",
    "test": "npm test --workspaces --if-present"
  }
}
