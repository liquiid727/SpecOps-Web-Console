# QA report — CLI-GUI-032 issue 103

Decision: **blocked**

Local component/static checks passed, but the blocking Test Spec evidence is incomplete: Provider/Deployment CRUD recovery states, write-only Secret negative scans, Chrome interaction, responsive/a11y assertions, and screenshots/traces are absent. Local Vitest does not substitute for those gates.

